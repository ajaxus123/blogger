<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag, tag_path(tag), class: 'tag_name' %>
      <%= link_to "Delete tag", tag_path(tag), method: :delete %>
    </li>
  <% end %>

</ul>